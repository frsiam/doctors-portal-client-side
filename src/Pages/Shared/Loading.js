import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn btn-warning btn-lg w-full h-[calc(100vh-70px)] loading text-white text-2xl">loading...</button>
        </div>
    );
};

export default Loading;